interface:
  display_name: "Design"
  short_description: "Accurate visual artifacts with content-specific taste and clear quality gates"
  default_prompt: "Use $design to turn the supplied content or data into an accurate, aesthetically strong visual artifact. Preserve source values exactly, frame the reader job, complete the Taste Contract before selecting a template or tool, build the lowest sufficient expression, inspect the real target surface, and report Lane, Proven, Not proven, and Next. Use Assured execution only when its risk triggers apply."
policy:
  allow_implicit_invocation: true
