interface:
  display_name: "Design"
  short_description: "Beautiful, accurate visual reports from immutable content and data"
  default_prompt: "Use $design to turn user-supplied content or data into a beautiful, accurate visual report. Confirm artifact type, goal, and use scenario; preserve raw sources exactly; route every derived calculation through reproducible code; then build from a registered production template and validate source, calculation, visual, and delivery quality."
policy:
  allow_implicit_invocation: true
